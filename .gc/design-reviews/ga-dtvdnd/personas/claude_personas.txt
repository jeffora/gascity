role-neutrality-zfc-guardian|1|Anders Holmqvist|zero hardcoded roles;Core role-neutrality;ZFC purity|Does the design guarantee Core assets contain no Mayor, Deacon, Polecat, Refinery, Witness, Boot, Crew, or Gastown references outside migration docs or absence-proving tests?;For each core-renamed asset (mol-do-work, dog prompt, jsonl-export, reaper, mol-dog-jsonl, mol-dog-reaper, mol-shutdown-dance), does the design specify exactly which role names, requesters, and notification targets are stripped while keeping the result behavior-equivalent for generic use?;Is role-neutrality enforced by a test that scans Core assets for forbidden role tokens rather than by reviewer vigilance?|a Core asset retains a Gastown role name, requester, or notification path after generalization;role-neutrality is asserted in prose but not enforced by an automated scan over Core assets;a split asset leaves role-specific text in the Core half instead of the Gastown half
behavior-preservation-auditor|1|Rosa Caldeira|Gastown behavior preservation;before/after inventory;no silent drops|For every asset generalized into Core, does the before/after inventory show the stripped Gastown behavior re-homed in gascity-packs/gastown with the order, formula, script effect, prompt instruction, or notification path it preserves?;Does the inventory cover the non-obvious carriers of behavior such as order triggers, script branches, template variables, and escalation or recovery flows rather than just file moves?;How does the design prove Gastown keeps its current orders, formulas, script effects, and role-specific recovery flows such as reaper and shutdown-dance escalation or jsonl-export daemon behavior rather than asserting equivalence?|a Core generalization strips Gastown behavior with no corresponding addition in gascity-packs/gastown;the before/after inventory lists file moves but omits behavioral deltas like triggers, branches, and notifications;equivalence is claimed without a verifiable artifact tying each stripped behavior to its Gastown home
pack-resolution-loading-engineer|1|Naveen Iyer|import resolution;required Core load;no implicit Maintenance or in-tree Gastown|How is Core made a required runtime pack in resolved config, and what is the resolution order when Core, Gastown, bd, and dolt all participate?;Does the design ensure new cities never resolve examples/gastown/packs or an implicit Maintenance import, and that gc init --template gastown imports gascity-packs/gastown explicitly?;What happens at load time when Core is absent, whether a hard failure, the narrowly scoped dev/test escape hatch, or a doctor-only warning, and is that consistent across CLI, init, and runtime?|pack resolution still falls back to in-tree examples/gastown/packs or auto-includes Maintenance;Core-required versus Core-optional behavior is inconsistent between init, doctor, and runtime load;the dev/test escape hatch widens into a general opt-out that real cities can trip
embed-materialization-build-reviewer|1|Petra Novak|builtinpacks registry;embed path move;downstream importers;auto-include retirement|When internal/bootstrap/packs/core moves to internal/packs/core, does the design inventory and update every importer including hook code, internal/builtinpacks/registry.go, cmd/gc/embed_builtin_packs.go, and embed.go files so the build stays green?;Does builtin materialization stop embedding and auto-including the retired Maintenance pack while still materializing Core, bd, and dolt correctly?;Are dependent consumers that reference Maintenance scripts such as examples/dolt and dolt-target.sh repointed to their new Core homes without breaking?|the bootstrap-to-packs path move leaves stale importers or embed paths that fail to compile;builtin materialization still auto-includes Maintenance after the pack is retired;examples/dolt or other packs keep dangling references to moved Maintenance scripts
migration-upgrade-path-reviewer|1|Camille Okafor|existing-city upgrade;legacy local paths;actionable migration|For an existing city that references packs/maintenance or packs/gastown local paths, what exact migration steps does the design provide, and are they surfaced by import-state diagnostics?;Does migration messaging stop claiming Maintenance is supplied implicitly and distinguish required Core from optional Gastown for the upgrader?;Is there a compatibility shim or grace behavior for internal/bootstrap/packs/core so pinned or in-flight references degrade gracefully rather than breaking abruptly?|existing cities on legacy paths get a hard failure with no migration instruction;diagnostics still describe Maintenance as implicitly supplied;the bootstrap core path is deleted with no shim while references may still point at it
doctor-diagnostics-ux-reviewer|1|Tobias Lindgren|missing-Core diagnostic;fix-command wiring;required versus optional clarity|When a resolved config lacks Core, does the diagnostic name the resolved config source that omitted it, explain why Core is required, and offer a concrete fix that adds the Core pack entry?;Is the fix actually wired to apply a config edit or only advisory text, and does it behave consistently across the doctor and import-state surfaces?;Do diagnostics clearly separate required Core behavior from optional Gastown behavior, and is the dev/test opt-out diagnosable without misfiring on real cities?|the missing-Core diagnostic is generic and does not identify the offending config source;the offered fix is prose only with no executable remediation path;required-versus-optional framing is muddled so operators cannot tell what is mandatory
test-enforcement-ci-reviewer|1|Mira Acharya|invariant tests;Maintenance auto-include removal;dog-not-hardcoded|Does the design add tests that fail if Core reintroduces Gastown role names or Gastown-only workflows rather than relying on reviewer vigilance?;Are tests that previously asserted Maintenance auto-inclusion rewritten or removed, and do new tests cover Core-as-required plus Gastown-as-explicit-external import?;Is there a test proving dog is allowed only as Core's configurable default agent and is not a hardcoded Go role?|role-neutrality and behavior-preservation invariants have no enforcing test;legacy Maintenance auto-inclusion tests are left in place and contradict the new model;dog overridability and non-hardcoding are left unverified by tests
asset-classification-split-reviewer|1|Hugo Bautista|split and core-renamed ownership;file-by-file migration table;clean halves|For each split asset such as the gc-dispatch skill, the maintenance README, and the architecture, following-mol, command-glossary, and tdd-discipline fragments, does the design define exactly which content goes to Core and which to Gastown with no orphaned or duplicated behavior?;Are the review-marked assets such as mol-review-quorum and the gastown codex hooks.json given a definite Core-or-Gastown owner with a stated rationale?;Is the migration documented as a complete file-by-file table so ownership is auditable by behavior rather than by current path?|a split asset is moved wholesale to one pack instead of being divided;review assets remain unresolved with no owner decided in the design;the file-by-file migration table is incomplete or omits classification rationale
dog-agent-config-reviewer|1|Kenji Watanabe|dog as pack default;config not Go;user override|Is the Core dog maintenance agent defined purely as pack configuration via agent.toml and prompt with no Go special-casing on the name dog?;Can a user override or replace the Core dog configuration through explicit config without editing Go code, and does the design show the override path?;Are the Core maintenance formulas that need agent execution such as reaper and jsonl tied to dog by configuration so removing or renaming dog is a pure config change?|any Go code branches on the agent name dog, reintroducing a hardcoded role;the Core dog agent cannot be overridden without source changes;Core maintenance behavior silently breaks if the dog agent is renamed or removed
requirements-traceability-scope-reviewer|1|Eleni Castellanos|AC coverage;scope discipline;no out-of-scope creep|Does the design trace every acceptance criterion from the five requirements user stories to a concrete design decision or artifact?;Does it respect Out Of Scope by avoiding MEOW redesign, role-model rework beyond relocation, and renaming of unrelated maintenance Go packages or config fields used for supervisor or store maintenance?;Are the requirements remaining open items such as fix-command wiring, test-helper opt-out shape, and review-asset ownership resolved in the design rather than deferred again?|one or more acceptance criteria have no corresponding design decision;the design renames unrelated maintenance Go identifiers or touches MEOW primitives;requirements-level open questions are restated as design open questions without resolution
