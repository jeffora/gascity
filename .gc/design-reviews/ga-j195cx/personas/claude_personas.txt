backcompat-migration-custodian|1|Marisol Vega|deprecation window;in-flight Pack v2 merge;contract removal trigger|How do externally distributed packs that pin contract = graph.v2 by SHA continue to compile after the alias is removed?;The proposal defers the contract removal until after a migration window — what is the measurable trigger and who owns it?;Does the rollout sequencing account for the in-flight Pack/City v2 merge described in formulas.md, or is requires layered on top of an unstable substrate?|removal of contract scheduled by calendar rather than telemetry of remaining usage;deprecation warning never reaches the surface where pack authors actually look;requires lands during the Pack v2 merge and entangles two unrelated migrations
sdk-architecture-invariant-guardian|1|Inga Sorenson|ZFC principle;layering invariants;internal formula authority|The doc says compiler-selection code should not check raw Contract — does the normalized requirement check live in exactly one place in internal/formula and is that enforced by tests?;Is requires.formula_compiler purely an assertion or does any code path use it as a selector and route to a different compiler?;Does relaxing version >= 1 alter any other invariants in formula validation, especially the convergence formula subset?|requirement-check helper duplicated across CLI, orders, sling, and API handlers;normalized check still co-exists with declaresGraphV2Contract as a deprecated alias;version-relaxation breaks an unrelated assumption in convergence or pack resolution
schema-evolution-architect|1|Devon Park|requirement grammar;reserved keys;future axes|V0 only accepts >=1 and >=2 — is the grammar genuinely a parser or a two-string allowlist that future ranges will have to re-architect?;Should bare 2 or 2.0 be accepted as sugar for >=2 or rejected, and what happens if v1 changes that answer?;Why namespace under [requires] rather than top-level — does the design anticipate other axes (tools, pack format, runtime), and how are unknown sibling keys handled?|string-typed expression with no documented grammar for future ranges;unknown keys under [requires] are silently ignored instead of rejected;[requires] reserved for compiler capability only with no plan for additional requirement axes
operator-error-surface-auditor|2|Tomas Hidalgo|multi-surface consistency;warning rate-limiting;layered daemon config|When [daemon] formula_v2 = false and a v2-required formula is dispatched, do CLI exit messages, dashboard error cards, and order-event payloads all carry the same actionable remedy text?;If a rig overrides daemon.formula_v2 inconsistently with the city, which one wins and does the error message describe the layered resolution truthfully?;Is the contract deprecation warning emitted once per session or once per resolve cycle, and how does that interact with poll-driven dispatch?|deprecation warning fires per scan and floods CI logs;dashboard and CLI emit different remedy text for the same root cause;layered daemon override silently chooses one side without telling the operator which
validation-matrix-auditor|2|Priya Anantharaman|parser edge cases;test parameterization;conflict matrix|The conflict matrix covers contract+requires agreeing or disagreeing — what about contract set to an unknown value plus requires = >=2, or empty [requires] table, or unknown sibling keys?;Is requires.formula_compiler = >=1 observably different from omitting [requires] entirely, and which path is canonical?;Are validation tests parameterized over the matrix or hand-written per case and likely to drift?|empty [requires] table or garbage expression slips through as accepted;v2-only construct check is a hand-coded list out of sync with the parser;>=1 and omitted diverge accidentally because two code paths handle them
caller-rewrite-consistency-reviewer|2|Yuki Tanaka|caller inventory;in-flight molecules;dead-code policy|Has every caller of declaresGraphV2Contract been inventoried across sling, orders, dispatch, API, dashboard, and convergence and migrated to one shared helper?;If [daemon] formula_v2 is flipped off while a v2-required wisp is in flight, are the running steps allowed to finish or rejected on next compile?;Will declaresGraphV2Contract be deleted outright or left as a deprecated alias when project conventions forbid zombie code?|requirement check re-implemented inline at multiple call sites;in-flight molecules left in undefined state when daemon flag flips;deprecated declaresGraphV2Contract retained as a wrapper indefinitely
staged-rollout-planner|2|Owen Driscoll|rollback granularity;built-in pack updates;migration-window definition|The 8-step plan has no rollback story — if step 4 (replace declaresGraphV2Contract) regresses dispatch, can step 4 alone be reverted?;Built-in and example formulas are updated in step 6 — are they shipped so older Gas City binaries still parse, or does the binary version need to bump in lockstep?;What measurable signal triggers the step-8 decision to remove contract — elapsed time, telemetry on warning emission, zero remaining external usage, or undefined?|built-in formulas updated in the same release as the parser change with no compatibility shim;rollback would require reverting multiple coupled steps as a single revert;step-8 trigger left as an unowned 'after a migration window' with no measurable definition
pack-ecosystem-author-advocate|3|Saoirse OMalley|external author workflow;migration guide;lint and validate|External pack authors copy contract = graph.v2 from current docs — what is the new canonical example and is there a gc formula validate or lint that catches stale snippets?;A pack pinned by SHA expects to keep compiling indefinitely — what runtime version is the last that compiles old contract without warning, and is that documented for pack authors?;Is there a recommended-reading link for pack authors explaining why version on a formula file is wrong and pack revision is the boundary?|no migration guide aimed at external pack authors beyond an SDK changelog entry;tutorials and getting-started examples not updated in the same PR as the design;no lint or validate command surfaced for pack authors
documentation-terminology-auditor|3|Avery Whitfield|term clarity;doc currency;open-question tracker|Capability vs implementation vs contract — is there a single source-of-truth glossary entry for compiler capability, or do new readers have to infer it from context?;docs/reference/formula.md still treats version and contract as standard top-level keys — is the doc update part of the same PR as the validation change?;The three open questions have user-visible consequences — should they be resolved before shipping rather than left as live ambiguities at merge time?|docs updated in a PR separate from the parser change so behavior and docs diverge;the term requires is overloaded with another concept already in pack.toml;open questions left unresolved at merge with no deferred-decision tracker
convergence-subset-reviewer|3|Felix Berger|convergence validator;subsystem coupling;subset drift|internal/convergence/formula.go validates a formula subset — does the new [requires] table participate in that subset or is it dropped silently?;Convergence-specific keys (convergence, required_vars, evaluate_prompt) are top-level — does this design create a precedent that new top-level keys keep accreting alongside [requires]?;If a convergence formula needs v2 capability, does the convergence loop respect [daemon] formula_v2 or bypass via its own dispatch path?|convergence validator silently drops the [requires] table;v2 capability gate enforced in some dispatch paths but bypassed by convergence;subsystem-specific formula subsets continue to drift from the canonical model
