# Apply Summary

Attempt: 17
Verdict applied: iterate
Synthesis: `.gc/design-reviews/ga-2404qu/attempt-17/synthesis.md`
Design updated: `.gc/design-review-inputs/core-gastown-pack-migration/design.md`

## Changes

- Added `### Attempt 17 Review Resolution Contracts` to convert the global
  `block` verdict into executable design contracts.
- Addressed the required Core loader blocker with a default-deny generated
  loader inventory, typed `RequiredSystemPackParticipation`, provider-pack
  selection, and loader-class failure matrix.
- Bound the public Gastown/Core/Maintenance model, including Core dog ownership,
  public Gastown patch behavior, prompt fragment rules, stale Maintenance
  rejection, and exact pinned asset ledgers.
- Expanded role-neutrality requirements to cover live Go, API/OpenAPI,
  dashboard, generated text, comments, prompts, scripts, templates, and
  ownership-aware allowlists.
- Added activation/cache/rollback requirements for three public pin rows,
  old/new binary evidence, stale synthetic cache handling, ordinary-cache
  promotion, downgrade artifacts, and zero-duplicate/no-gap gates.
- Split `gc doctor` report-only behavior from `gc doctor --fix` mutation and
  added coordinator, publish, runtime-state, custom-pack, and zero-write
  healthy-fix requirements.
- Defined schemas, owners, freshness tests, commands, and independent
  completeness checks for generated behavior, role-surface, loader, slice-gate,
  pin, wording, test-migration, and provider-exception artifacts.
- Added bootstrap hidden-dependency guards for symbols and paths, including
  `GC_BOOTSTRAP`, production embed removal, non-nil empty bootstrap assets, and
  Core skill materialization from system-pack sources.
- Bound operator docs, terminology, generated wording lint, release transcript
  evidence, behavior witness rows, and provider-pack continuity exception rows.

## Unfixable Items

None. The design remains `iterate` because the attempt-17 global verdict was
`block`, and the next review must verify the new contracts.

