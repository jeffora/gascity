behavior-preservation-auditor|1|Mara Volkov|Gastown behavior parity, before/after inventory enforcement, generalization loss, release gate|How is the behavior inventory verified mechanically rather than by author assertion, given it lives in a different repo than the code being stripped?;Which concrete test fails if a Gastown requester or notification path stripped from a Core-bound asset (mol-dog-reaper, mol-shutdown-dance, jsonl-export) is never re-added in gascity-packs/gastown?;What defines "equivalent behavior" per inventory entry — same notification targets and triggers, or just same general capability?|inventory is a markdown file with no test or CI linkage;Core generalization lands before the Gastown replacement exists in a released gascity-packs commit;behavior-preservation tests assert file existence rather than observable behavior
cross-repo-release-engineer|1|Devon Okafor|two-repo rollout sequencing, PublicGastownPackVersion pinning, version skew, rollback|What does a fresh gc init --template gastown produce between rollout step 1 (gascity-packs lands) and step 2 (Gas City pin update), and is every intermediate state shippable?;Is PublicGastownPackVersion an immutable commit hash verified at materialization, or a mutable ref that can drift?;How do you roll back if the Gas City registry change is reverted after the gascity-packs release is already public and cities have fetched it?|no CI gate or compatibility test linking the two repos at the pinned version;rollout steps depend on a human remembering to update the pin/hash;removing the gastown synthetic cache breaks offline or air-gapped init with no stated fallback
upgrade-path-guardian|1|Priya Raghunathan|existing city migration, doctor fix rewrites, legacy imports, order skip lists, stale system packs|What does an existing city with legacy Maintenance/Gastown imports see on the first gc start after upgrade, before anyone runs gc doctor --fix — does it run degraded, fail loud, or fail silent?;Are all moved order and formula names byte-identical so existing order skip lists and references keep working, and which migration test proves each one?;What happens to a city whose operator edited files under .gc/system/packs/maintenance that the new code now ignores?|a working city requires a manual doctor --fix to keep functioning after binary upgrade;an order or formula is renamed without a compatibility alias or migration test;doctor fix removes imports or files in a way that silently orphans operator customizations
core-loading-invariant-reviewer|1|Hank Stillwater|required Core invariant, config load call-site audit, production bypass paths, test escape hatch scope|Which durable enforcement (guard test or lint) guarantees no production cmd/gc path calls config.LoadWithIncludes without the system-pack wrapper, rather than a one-time manual audit?;Can a partially materialized or tampered .gc/system/packs/core yield a successfully loaded config that silently lacks Core orders until a doctor run notices?;How does the test-only no-Core escape hatch stay structurally unavailable to production code if it is just a lower-level loader anyone can call?|the call-site audit is a one-time pass with no test preventing regression;Core absence is detectable only by doctor rather than surfacing at config load time;_test helper bypass patterns appear in non-test files
zfc-architecture-guardian|2|Ingrid Maelstrom|ZFC, zero hardcoded roles, dog special-casing, SDK self-sufficiency, controller-driven infrastructure|Does any Go change in this migration introduce role-conditional logic, including treating the literal name "dog" as a default in code rather than pack configuration?;Does the Core role-name guard test scan every asset type — scripts, overlays, orders, template fragments, doctor checks — or only formulas and skills?;Do any Core orders or formulas require a specific configured agent to exist, violating SDK self-sufficiency when an operator removes the dog pool?|dog referenced in Go source rather than exclusively in pack TOML and templates;the role-name guard allowlist grows to excuse convenient violations;a Core infrastructure order breaks when the [[agent]] entry it assumes is removed
test-migration-strategist|2|Tomas Eriksen|coverage continuity, gastown_test.go relocation, registry test pinning, fixture drift|Which Gastown behaviors lose all CI coverage in the window between deletion of examples/gastown tests in Gas City and recreation of equivalents in gascity-packs?;Do the rewritten builtinpacks and embed tests assert loaded behavior — orders resolve, formulas parse, dog agent configures — or just pin path lists and counts?;How does the proposed internal/bootstrap testdata core fixture stay in sync with the real internal/packs/core, and what detects drift?|pack tests deleted in the same change that moves assets with no linked replacement landing first;coverage asserted by file existence or include counts only;a test fixture copy of Core that silently diverges from the shipped Core
doctor-semantics-reviewer|2|Sofia Mbeki|doctor check correctness, fix idempotency, severity convention, destructive fix safety|Is the core_pack_doctor_check fix idempotent and provably a no-op on a healthy city, including when run concurrently with a running controller?;When the import-state fix removes a redundant explicit Core import, what confirms the required system Core is present first, and what happens if that confirmation races a partial materialization?;How does the check distinguish corrupt materialized Core from intentional operator overrides layered on top, and does the unsupported custom-core path stay manual rather than auto-fixed?|a fix that rewrites city.toml or pack.toml beyond its stated scope;Error versus Warning severity inconsistent with the existing required-repairable convention;the unsupported custom Core import path silently treated as fixable
supply-chain-security-reviewer|2|Rafael Donato|external pack trust, fetch integrity, pin verification, embedded versus remote provenance|Is the public gascity-packs/gastown import verified by content hash at fetch and materialization time, and what rejects a tampered or force-pushed upstream?;What is the blast radius if the gascity-packs repo is compromised between pin updates — which cities re-fetch and when?;Does doctor --fix rewriting a local import to a remote public source constitute a trust escalation that needs explicit operator consent messaging?|pinning by mutable tag or branch instead of verified hash;no manifest or hash validation for fetched external packs analogous to the synthetic cache validation being removed;auto-rewrite of imports to remote sources with no consent or provenance note
docs-dx-reviewer|3|Lena Haugen|docs accuracy, migration messaging consistency, troubleshooting paths, tutorial coherence|After migration, do all documented paths under .gc/system/packs match exactly what a fresh init and doctor --fix actually produce?;Is the "maintenance is retired; Core supplies generic maintenance" wording identical across doctor output, import-state messages, docs, and pack.toml comments, or will operators see three different stories?;Can a new operator complete tutorial 01 and the troubleshooting guide without hitting a single reference to the removed Maintenance pack or in-tree Gastown pack?|docs updates deferred to a separate later change instead of landing with the behavior;doctor message wording diverging from documentation wording;examples/gastown left half-migrated as neither a working example nor removed
operational-longevity-reviewer|3|Marcus Tidewell|long-term ownership, dual-repo maintenance burden, dolt temporal coupling, legacy directory hygiene|Who owns keeping Core dog assets and the Gastown dog patch compatible when either side changes, and is that patch dependency named in the inventory as required?;What is the concrete trigger and tracked work item for moving dolt-target.sh out of Core when bd provider support returns, so "for now" does not become permanent?;How do ignored legacy .gc/system/packs directories eventually get cleaned up, given fix must not delete them — advisory forever, or a documented operator runbook?|temporary placements (dolt-target.sh, examples/gastown remnants) with no tracking issue or revisit trigger;Gastown patching Core agent internals it does not own without a declared contract;legacy-directory advisories that accumulate indefinitely with no resolution path
