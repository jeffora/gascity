mutation-bypass-path-auditor|1|Elena Marchetti|production bypass audit;mutation boundary enforcement;patch-map escape hatch;call-site inventory completeness|Which production call sites in cmd/gc/session_reconcile.go and internal/api/session_resolution.go currently write session lifecycle metadata directly, and does the backlog specify exactly which ones migrate per slice rather than leaving some in permanent exception status?;If patch helpers remain exported for test and doctor use, what prevents a future production caller from importing and calling them, given that Go's internal packages are not used and there is no failing-build import guard specified?;Does the design's exception list for doctor, migration, and conformance utilities define a scope boundary, or does it become an open escape hatch that new production code can self-label into?;Production call sites remain that write lifecycle metadata directly after the command boundary ships, with no CI guard to catch regressions;Exported patch helpers lack any static or runtime enforcement and become de facto external APIs;The mutation boundary inventory misses write sites in reconciliation, drain, or scale paths
decider-purity-and-atomicity-enforcer|1|Takeshi Yamamoto|decider purity enforcement;commit-emit rollback ordering;compare-and-swap semantics;concurrent mutation convergence|Can every proposed decider be called without importing beads.Store, runtime.Provider, or any I/O adapter, or do any slices need to query store state mid-decision making the pure-function claim false?;When a decider returns both a SessionMutation and a RuntimeIntent, what is the commit-emit-rollback contract if the runtime intent fails after the durable mutation has committed to the bead store?;How does the design prevent two concurrent controller passes from both reading stale facts, both deciding to wake the same session, and both applying conflicting mutations without compare-and-swap?;A decider implicitly depends on store queries or runtime probes, violating the pure-function invariant;Post-commit in-process event emission has no replay or recovery path if the process crashes between commit and emit;Concurrent reconciler passes re-decide and re-apply the same operation because facts go stale between read and commit with no version check
event-fact-vs-command-separator|1|Amara Osei|event payload factuality;subscriber idempotency;commit-emit durability;work-release guarantee|SessionWakeRequested is described as an event, but does its payload carry enough stable context for the reconciler to decide idempotently without querying live session state, or does it encode an implicit command to start?;If SessionClosed fires after bead mutation commits but before in-process subscribers receive it, and the controller crashes, what mechanism replays that event so work assignment release does not permanently skip?;Does the design specify that RuntimeIntent is a separate type from SessionEvent so provider-specific adapter fields never leak into the session event namespace?;Events carry imperative instructions for subscribers instead of stable facts, requiring subscribers to re-query state and creating tight coupling;Post-commit in-process events are lost on crash with no replay mechanism, breaking work-release guarantees;RuntimeIntent and SessionEvent share enough structure that they get conflated in caller code or adapter implementations
migration-collision-and-coexistence-analyst|2|Ravi Krishnamurthy|worker boundary coexistence;extraction sequencing;legacy-new command coexistence;split-brain prevention|The worker-boundary migration is already in flight on overlapping call sites in cmd/gc and internal/api — does the backlog specify the sequencing dependency between these two migrations so they do not create two half-finished boundaries?;During adoption, both legacy patch-map callers and new command callers will write session metadata — what prevents split-brain where some lifecycle writes bypass the command boundary while others go through it?;Which single backlog slice is independently shippable and revertible on its own, and what proves it does not silently require the next slice to be correct first?;Two half-finished migrations collide on the same call sites, leaving production code with neither boundary fully enforced;Legacy and command callers both write the same metadata fields during transition with no defined ownership, causing split-brain state;A slice cannot ship without converting every call site in one change, creating a hidden flag day
api-compatibility-and-projection-guardian|2|Sarah Chen|API response preservation;projection helper extraction;CLI compatibility;worker boundary routing|Does the design specify that API and CLI callers must project session decisions through new command APIs rather than re-implementing lifecycle logic from raw metadata fields?;When the target classifier changes how session_name and alias resolution work, what guarantees that existing API response shapes and CLI output formats remain identical for all scenario rows in REQUIREMENTS.md?;Are the documented root exceptions for direct session.Manager construction in internal/api and cmd/gc stable and bounded, or does the backlog create new exceptions as extraction progresses?;API handlers duplicate session lifecycle logic from raw metadata instead of using session-owned projection commands;Target classification changes API response shapes or CLI output formats without updating scenario tests;New cmd/gc production code imports session.NewManager directly, violating the worker boundary import test
reconciler-boundary-and-fact-isolation-auditor|2|Liam Okonkwo|reconciler extraction boundary;work-query and store-aggregation scope;health gate fact flow;pool scaling fact availability|Slice 6 says the reconciler keeps scheduling, budgets, and trace output while session owns health decisions — which specific health-decision functions move into session deciders and which scheduling functions remain, and is that boundary a clean split or a shared helper?;When a session decider needs pool size or work count facts, does it receive them as pre-computed immutable SessionFacts, or does it call the bead store directly, violating the fact-reader boundary?;Are provider health gates and wake/hold/drain eligibility extracted to separate deciders with separate fact readers, or do they share reconciler helper code that straddles both packages?;Health decisions move into session but still import reconciler helper types, creating a package dependency cycle;Session deciders query the bead store directly for work counts instead of receiving pre-computed facts;Wake eligibility and health gates share reconciler helper code that cannot be extracted into either package cleanly
test-parity-and-regression-strategist|2|Natasha Volkov|decider table-test coverage;characterization test capture;proof-citation freshness;scenario-ledger alignment|Each backlog item lists existing proof test files — are those tests being repurposed as parity guards for the new command boundary, or must brand-new decider tests be written, and does the design state which for each slice?;When logic moves from reconciler or manager methods into deciders, what process keeps cited proof paths from going stale or pointing at deleted coverage?;Does the design require characterization tests to be captured before the first extraction so that behavioral regressions are detectable, or does it rely only on unit tests of the new code?;Decider tests require a real bead store or runtime, contradicting the TR-003 purity claim and making table-driven testing impossible;Proof citations point at tests that no longer cover the scenario row after logic moves to a new file;No characterization or golden tests are captured before the refactor begins, making regressions invisible until production
backlog-dependency-and-YAGNI-sentinel|3|Kwame Asante|backlog slice independence;YAGNI scope enforcement;premature event-sourcing avoidance;broad facade prevention|TR-007 designs event payloads for future durability while explicitly deferring event sourcing — is that future-proofing already bending today's API shape against the no-premature-abstraction rule stated in the non-goals?;The design introduces six new vocabulary types up front — are all of them genuinely needed for slice 1 target classification, or do some only serve later slices and violate YAGNI?;Which technical requirements are testable and exercised now versus aspirational, and should the aspirational ones be cut from the first implementation to prevent scope creep?;New types or vocabulary introduced in slice 1 are not actually used until slice 4 or later, violating YAGNI;Event payload shape is contorted for a hypothetical durable event log that may never be built;The backlog says one-at-a-time but slices 4 and 5 share reconciler helper functions that cannot be extracted independently
operability-and-observability-reviewer|3|Ingrid Holm|decider output observability;conflict reason traceability;health patrol integration;operator diagnostic path|Can an operator explain why a session was blocked, woken, or drained from decider output alone, or does diagnosis require reading raw bead metadata?;Do SessionConflict values carry enough context for gc trace and doctor flows to identify the blocker without querying additional state?;After health gates and progress facts move into session-owned decisions, does the reconciler still emit enough trace output for operators to diagnose scheduling and budget decisions?;Decider results lack blocker or wake-cause details, making operator diagnosis require raw bead inspection;SessionConflict types collapse multiple failure modes into generic errors, losing diagnostic context;Health and progress gate extraction removes observability from the reconciler without replacing it in session-domain output
documentation-alignment-and-scenario-sync|3|Priya Sharma|REQUIREMENTS.md scenario coverage;design-doc and proof test alignment;migration documentation sequencing;acceptance criteria testability|Which REQUIREMENTS.md scenario rows are touched by each backlog slice, and does the design map them explicitly or leave the mapping implicit?;When an extraction moves logic between files, does the design specify a process for keeping cited evidence paths current, or do citations silently go stale?;Are the acceptance criteria for each technical requirement concrete enough to be tested, or are some stated as aspirations that cannot be verified by a CI check?;Backlog items lack explicit mapping to scenario rows, making it impossible to verify that extraction preserves product behavior;Acceptance criteria for TR-002 and TR-007 describe aspirations without testable thresholds;Design docs and REQUIREMENTS.md drift apart because no process reconciles them after each slice
