behavior-parity-regression|1|Dr. Ingrid Voss|scenario-ledger parity, regression prevention, TR-001 enforcement, extraction safety|How does each extraction slice prove parity with the SESSION-* scenario rows it touches before the legacy path is removed?;Which scenario rows lack tests strong enough to catch a behavior change introduced by a decider rewrite?;What is the rollback story when a parity gap is discovered after callers have adopted a session-owned command?|slice merged with decider tests but no parity test against an existing scenario row;scenario ledger updated to match new behavior without owner sign-off;legacy path deleted in the same change that introduces its replacement
dual-path-migration|1|Marcus Adeyemi|incremental migration, dual-write drift, adoption sequencing, legacy-path retirement|While old metadata writes and new commands coexist, what prevents the two paths from diverging on the same session bead?;What is the explicit completion criterion for each backlog slice, and who tracks call sites that still bypass commands?;How is new code prevented from adding fresh direct SetMetadata lifecycle writes during the multi-release migration window, including while the in-flight worker-boundary migration touches the same call sites?|no static guard or test forbidding new external lifecycle metadata writes;backlog slice declared done while known bypass call sites remain unowned;command API and patch-map path both mutating the same fields with different validation
event-delivery-consistency|1|Priya Raghunathan|post-commit events, crash consistency, lost-event recovery, idempotent subscribers|If the process crashes after a session mutation commits but before in-process SessionEvent delivery, how does the work-release subscriber required by SESSION-WORK-001 still run?;Which subscriber reactions are safety-critical such as stranded-work release versus best-effort, and where is that distinction recorded?;How do subscribers reconcile from current session facts when an event is missed, per the NDI convergence principle?|work release depends solely on observing an in-process event;event payloads lacking stable identifiers needed for idempotent replay;subscriber performing cross-domain mutation with no convergent reconciliation fallback
concurrency-races|1|Chen Wei-Lun|optimistic concurrency, command validation races, reconciler interleaving, idempotence|Between fact gathering and mutation commit, what detects a concurrent state change by the reconciler, API, or another CLI actor?;Can a decision computed from stale SessionFacts ever be applied, and what compare-and-swap or revalidation exists in the applier?;When a decision yields both a SessionMutation and a RuntimeIntent, what is the commit, emit, and rollback order if the runtime action fails after the durable mutation committed?|applier validates state but commit is not conditional on the validated snapshot;facts gathered once then reused across multiple dependent decisions;no defined ordering or rollback contract between mutation commit, event emit, and runtime intent execution
contract-scope-abstraction|2|Sofia Lindqvist|API surface design, vocabulary cohesion, facade creep, premature abstraction|What stops SessionFacts from accreting every field any decider might want and becoming the broad facade the non-goals forbid?;Are SessionDecision and SessionConflict expressive enough for API error mapping without leaking session internals to callers?;Which of the six vocabulary types are proven by at least two real call sites in slice one, and which exist only for later slices?|one mega-facts struct shared by unrelated deciders;typed conflict reasons that collapse to a generic error at the API edge;vocabulary types or event-log future-proofing introduced before any slice uses them
boundary-enforcement-layering|2|Tomás Herrera|mutation boundary enforcement, layering invariants, import guards, exception scope|Which automated guard fails the build when production code outside internal/session writes lifecycle or identity metadata, and how does it compose with the existing worker-boundary import test?;How are the doctor, migration, and repair exceptions scoped so they do not become a loophole for ordinary callers?;Does the decider and command split respect the no-upward-dependency rule when cmd/gc and internal/api adopt session-owned commands?|boundary documented in prose but not enforced by a failing-build guard;exception list growing without review or expiry;session package importing work, mail, extmsg, or adapter types to shape decisions
target-classification-semantics|2|Amara Diallo|targeting taxonomy, identity resolution, alias safety, misrouting prevention|Does the classifier enumerate every category current callers distinguish, including ambiguous and conflict cases, with one shared precedence order matching SESSION-ID-003?;How is misrouting prevented when an alias, a config name, and a template name overlap on the same input token?;Which caller behaviors change when classification centralizes across API, mail, assignee normalization, and extmsg, and how is each divergence caught before adoption?|classifier output reinterpreted differently per caller;resolution order in the classifier differing from the SESSION-ID-003 ledger row;mail or extmsg accepting a target category the API rejects
pure-decider-testability|2|Yuki Tanimoto|functional core purity, table-driven coverage, clock injection, fact completeness|Are clock values and policy durations always injected as facts, with no hidden time.Now or config reads inside deciders?;Do decider tables cover the full blocker and wake-cause matrix from the canonical projection vocabulary, including conflict rows?;When a decider needs a fact that was not gathered, such as mid-decision runtime liveness, does the design fail closed, fail open, or force a new fact family?|decider reaching into store, runtime, or environment during evaluation;table tests asserting only happy paths while conflict and blocker rows go untested;deciders re-deriving lifecycle interpretation that ProjectLifecycle already owns
operability-diagnostics|3|Rosa Jiménez|conflict observability, trace integration, doctor compatibility, operator debugging|When a command rejects with SessionConflict, what do gc trace and the event log show an operator diagnosing a stuck session?;How do doctor and repair flows interact with the new mutation boundary without becoming permanent bypasses?;Can an operator distinguish a decision that was never taken from an event that was never delivered?|conflicts returned to callers but never logged or traced;decision inputs unrecorded, making stuck-state postmortems guesswork;doctor writing lifecycle metadata through the legacy path after commands exist
performance-reconciler-load|3|Viktor Baranov|fact-gathering cost, reconciler throughput, event fan-out overhead, store read amplification|What is the per-pass cost of materializing immutable fact structs for every session in a large city compared with today?;Do post-commit events add synchronous subscriber work to latency-sensitive paths like wake, close, and parallel lifecycle starts?;Where are fact reads batched or cached, and how is staleness bounded when they are?|per-session store round-trips replacing batched queries during fact gathering;synchronous event fan-out blocking the applier commit path;fact copies of large metadata maps allocated in hot reconciler loops
