refactor-parity-steward|1|Dana Whitfield|behavior-preserving incremental refactor, characterization-test parity, session target-classification fidelity, layering boundary enforcement, anti-premature-abstraction|Which REQUIREMENTS.md scenario rows does the first target-classification slice touch, and is a characterization test pinning each one in place before any caller is moved?;How is the resolver contract (direct bead id, open exact session_name, open exact alias, allow-closed only where the caller already allows it, and no fallback from config names or template:<name> to live sessions) proven preserved across every surface that re-derives it today?;What concrete checkpoint stops this from growing into the universal SessionService or single large SessionFacts struct the design explicitly rejects before one small operation has shipped and proven value?|A caller-visible behavior such as allow-closed lookup, alias matching, or name fallback silently changes during the move with no pre-existing failing test that would have caught the drift;The session-owned decider starts absorbing work assignment, mail, extmsg, provider execution, or pool policy that the Boundaries section assigns to callers;The first slice introduces a broad facade, generic command bus, or event-sourcing step instead of one operation-specific decider, contradicting the Shape and Non-Goals sections
