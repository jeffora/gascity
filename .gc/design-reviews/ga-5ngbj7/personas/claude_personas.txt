reconciliation-ledger-integrity|1|Dr. Karin Albrecht|specification verifiability, evidence integrity, reconciliation drift, testable invariants, canonical vocabulary discipline|Does every scenario row cite evidence that actually exists and provably enforces the stated required behavior, rather than a stale, missing, or weaker-than-claimed citation?;Is each global invariant (idempotent reconciliation, bead-backed truth, projection-only reads) pinned by an enforceable test that fails when code violates it, or is it only aspirational prose?;Which behaviors in lifecycle_projection.go and state_machine.go have no corresponding row, and how would a future change silently drift the ledger from code and tests?|A row whose Evidence cites a deleted or renamed test, or a bare commit hash with no live assertion of the claimed behavior;A required-behavior cell written as vague prose such as "where tests allow" or "when the tests require" that is not an independently testable input-to-output statement;The Canonical Vocabulary list diverging from the actual enums in lifecycle_projection.go, so the ledger mandates terms the code no longer emits
