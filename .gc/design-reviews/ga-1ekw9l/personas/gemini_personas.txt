cross-repo-evidence-chain-auditor|1|Oleg Marchetti|Gastown behavior inventory completeness;cross-repo evidence chains;silent capability regression|Does the behavior inventory enumerate every Gastown-specific requester, detector, notification path, and script branch removed from Core, with a corresponding gascity-packs/gastown landing commit that restores it?;When a Core-bound asset is generalized, what test in gascity-packs proves the Gastown behavior still fires under the same trigger conditions, rather than merely existing in a file?;Are there Gastown overlay fragments, script branches conditioned on role names, or prompt template conditionals that the inventory omits because they are not top-level pack assets?|inventory lists files without asserting observable behavior;Gastown landing commit lags behind the Gas City removal;Gastown test asserts file existence rather than triggerable behavior
required-core-loading-invariant-reviewer|1|Camille Sato|production Core inclusion;config resolution provenance;bypass path containment;test escape hatch scope|Which enforcement guarantees that every production cmd/gc path calls config.LoadWithIncludes through the system-pack wrapper, and what test fails if a new command bypasses it?;If a partially materialized Core pack exists under .gc/system/packs/core with a corrupted manifest, does config loading fail loudly or silently proceed without Core orders?;Does the test-only no-Core escape path use a lower-level loader that is structurally unreachable from cmd/gc, or is it just a flag that any caller could pass?|Core absence detectable only by gc doctor rather than surfacing at config load time;test bypass pattern appears in a non-test file;a new cmd/gc entry point forgets to route through the system-pack wrapper
doctor-fix-irreversibility-reviewer|1|Faisal Khoury|doctor fix idempotency;import rewrite safety;concurrent fix resilience;operator data preservation|Is the core_pack_doctor_check fix a proven no-op on a healthy city, and does it hold under concurrent gc doctor --fix and gc start?;When the import-state fix removes a redundant explicit Core import, what prevents it from also removing a user-added import that happens to look like a system-generated one?;If gc doctor --fix rewrites a local gastown import to the public remote, does it verify the remote is reachable and pinned by content hash, or does it leave the city in a broken state on air-gapped machines?|fix that mutates city.toml beyond its stated scope;import rewrite assumes network availability;stale .gc/system/packs directories accumulate indefinitely with no resolution path
zfc-role-leak-detector|2|Sanela Kovac|zero hardcoded roles in Go;dog exception containment;Core asset role neutrality;SDK self-sufficiency|Does any Go change in this migration introduce a conditional on the literal string "dog" in business logic, and if so, is it confined to a test or migration helper rather than production code?;Does the role-name guard test scan every Core asset type — scripts, overlays, orders, template fragments, doctor checks — or only formulas and skills?;Can any Core order or formula break when the dog [[agent]] entry is removed from city.toml, and what test proves that?|dog referenced in Go source outside pack configuration;role-name guard allowlist grows to excuse convenient violations;Core infrastructure order assumes a specific agent pool exists
cross-repo-version-skew-analyst|2|Tomoko Hayashi|two-repo rollout sequencing;public pack pin integrity;rollback granularity;intermediate state safety|What does a fresh gc init --template gastown produce in the window between the gascity-packs landing and the Gas City PublicGastownPackVersion pin update, and is that state deployable?;Is PublicGastownPackVersion pinned to an immutable content hash, or can a mutable ref drift to a different commit between materialization and verification?;If the Gas City registry change is reverted after operators have already fetched the new gascity-packs commit, can they roll back without losing local Gastown behavior?|pinning by mutable tag or branch;gascity-packs landing requires a simultaneous Gas City change;rollback path requires operator manual intervention not documented in the migration
pack-registry-completeness-reviewer|2|Marcus Lindqvist|builtin pack list changes;synthetic cache tamper detection;source path migration;provider-dependent pack behavior|Do registry tests assert that only core, bd, and dolt are in the embedded pack set, with Core sourced from internal/packs/core and no gastown or maintenance aliases?;When MaterializeBuiltinPacks repairs a corrupted Core, does it preserve the exact behavior of provider-dependent bd and dolt packs without re-materializing them unnecessarily?;Does the synthetic cache validation reject a tampered .gc/system/packs/core directory that contains extra files or modified manifests?|old internal/bootstrap/packs/core source still accepted silently;bd or dolt inclusion behavior changes during Core repair;synthetic cache test only checks file existence, not content integrity
bootstrap-test-isolation-reviewer|2|Ritu Balasubramanian|production Core removal from bootstrap;test fixture drift;escape hatch containment;hidden asset dependencies|Does removing production Core from BootstrapPacks break any test that relies on AssetDir packs/core, and is the replacement fixture explicitly isolated from the real pack?;Are tests that need no-Core behavior using a lower-level config loader rather than setting a runtime flag or environment variable?;Does any cmd/gc test import internal/packs/core indirectly through a path that should now use the system-pack wrapper?|test-only escape hatch becomes a user-visible flag;bootstrap test exercises real Core pack instead of intended fixture;removing internal/bootstrap/packs/core breaks hook installation through an unstated dependency
pack-boundary-split-reviewer|2|Ewan McAllister|Core vs Gastown asset ownership;Maintenance retirement correctness;Gastown self-sufficiency;split completeness|Which concrete test proves that a city running Core plus public Gastown, with no Maintenance pack, preserves all Gastown-specific behavior including Polecat, branch pruning, and reviewer workflows?;Do all Gastown pack comments and imports reference only host-required Core, with no vestigial Maintenance references?;When a Core asset is generalized (e.g., gc-dispatch, mol-shutdown-dance), does the split ensure the Gastown-specific branch is fully present in gascity-packs rather than conditionally reachable from a shared template?|Gastown still imports or documents Maintenance;shared asset contains Gastown-specific conditional logic rather than a clean split;Gastown test removed from Gas City without replacement coverage in gascity-packs
migration-dx-consistency-reviewer|3|Ines Moreau|terminology consistency across docs and CLI;maintenance word disambiguation;tutorial path integrity;operator error messaging|Are the terms "Core", "Gastown", and "retired Maintenance" used identically in doctor output, import-state messages, docs, and pack.toml comments, or will operators encounter three different narratives?;Can a new operator complete tutorial 01 without encountering a reference to the removed Maintenance pack or the retired in-tree Gastown?;Do troubleshooting docs guide operators from "my orders are missing" through "check .gc/system/packs/core integrity" rather than "check .gc/system/packs/maintenance"?|doctor message wording diverges from documentation wording;examples/gastown left in a half-migrated state that neither works nor is removed;store-maintenance documentation accidentally rewrites Dolt maintenance instructions
test-slicing-coverage-reviewer|3|Dimitri Volkov|migration step test ordering;coverage continuity during asset moves;fixture drift detection;acceptance traceability|Is the migration sliced so each individual commit keeps make test-fast-parallel passing, or does any step require a cross-repo state that only exists after both repos land?;Do rewritten builtinpacks and embed tests assert loaded behavior — orders resolve, formulas parse, dog agent configures — rather than just counting files or checking include paths?;How does the proposed internal/bootstrap testdata core fixture stay in sync with the real internal/packs/core, and what CI gate detects divergence?|asset move lands without a passing intermediate test state;test coverage relies on file counts or include paths rather than observable behavior;fixture copy of Core diverges from shipped Core with no detection mechanism
