required-core-loader|1|Priya Venkataraman|required-Core loader contract, typed RequiredSystemPackParticipation, deny-by-default loading, bypass scanner, fail-closed runtime gates|Does the two-gate model compose with every behavior-driving load path in loader-inventory.generated.yaml, including indirect config.Load calls through wrappers, function values, or API state composition that a token scanner cannot see;When strict pre-resolution integrity fails during a live controller no-refresh reload, does fail-closed brick a running city or is there a defined degraded path;Can participation be proven for a pack that contributes zero agents through a real resolved-config import edge, without a synthetic record that quietly satisfies the gate|Bypass scanner is token or regex based and misses indirect loader calls through aliases or helper packages;Post-resolution participation collapses into a renamed path or digest check rather than a true resolved-config edge;Behavior is undefined when Core is valid but a provider host pack is missing, stale, or shadowed
behavior-preservation|1|Marcus Eilenberg|source-derived behavior manifest, packcompat evidence floor, Gastown behavior loss, cross-repo pin coupling, witness completeness|What proves the manifest generator reference-following across script, template, prompt, order, mail and nudge targets is complete, and what catches a behavior that has no discoverable trigger in the old tree;How does CI avoid a circular dependency where the activation pin cannot be validated until the behavior it is meant to prove has already moved to gascity-packs;Is there an executable check that each new witness actually exercises the original trigger through normal resolution rather than asserting mere existence|The manifest is effectively hand-curated despite the generated framing, with no generator spec or owner;A moved behavior that had no old test silently requires no witness at all;gascity-packs landing is assumed but Gas City CI cannot actually fetch and verify the exact pin in the activation slice
doctor-data-safety|1|Sofia Reinholt|doctor mutation coordinator, multi-file failure-atomicity, byte-preserving TOML edits, compare-before-rename, crash-released advisory lock|Is the city advisory lock held continuously across the entire stage-to-publish window, and what is the recovery when the lock is lost on crash or a networked filesystem between stage and publish;Does the scoped byte-preserving TOML edit planner already exist as a real approach, and what test proves the refuse-instead-of-rewrite fallback actually fires on un-editable manifests;Per-file rename is atomic but cross-file is not, so how is a crash between the second and third file rename recovered without leaving the city half-migrated|The cross-file failure-atomic claim conflates per-file rename atomicity with a real multi-file transaction;An advisory lock cannot constrain a second old-binary process that never takes the lock;Byte-identical-on-failure covers only preflight failures and not a crash during the publish phase
role-neutrality-zfc|1|Anand Krishnaswamy|zero hardcoded roles, ZFC, core.maintenance_worker binding, dog-as-configuration, SDK self-sufficiency|After the binding indirection, does any Go control flow still branch on the resolved worker identity, or is it purely transport through run_target_binding and environment as ZFC requires;With the dog agent entry removed entirely, do all controller-owned SDK operations still function, satisfying the rule that no SDK feature depends on a configured agent;What stops historical-fixture and compatibility-row allowlist classifications from accumulating live role logic forever, and is row expiry actually enforced by a failing gate|dog survives as a Go special case somewhere such as theme selection, classifyAgentKind, or warmup mail defaults;The binding still encodes a judgment call in Go of the form if worker omitted then alter behavior;Allowlist rows have no enforced expiry, so role debt becomes permanent under a neutrality banner
pin-cache-supply-chain|1|Lena Hoffmann|public Gastown pin, synthetic alias retirement, RepoCacheKey identity, offline init, pins.yaml ledger authority|What guarantees stale synthetic-alias cache entries can never be selected by any path including lock refresh and import check or install, not merely that they are labeled stale;How is the one-time cache-promotion helper promoted entry verified to match the immutable commit digest, rather than laundering stale bytes into a trusted ordinary cache key;Is the pins.yaml ledger validator a real CI gate that fails the build when PublicGastownPackVersion and the ledger disagree, and who freezes the immutable commit|A stale on-disk cache can still win a resolution race in some path not covered by negative tests;Offline fresh-init failure is asserted but an old-binary offline upgrade silently falls back to embedded Gastown bytes;The immutable cross-repo commit is aspirational with no proof gascity-packs tags and freezes it before Gas City pins it
rollout-slicing|1|Tomas Beckett|seven-slice rollout, independently deployable slices, paired cross-repo boundary, no-Maintenance gate ordering, rollback matrix|Is slice 5 updating the activation pin and removing Maintenance from requiredBuiltinPackNames genuinely one revertable commit, or an irreversible cross-repo boundary once gascity-packs is consumed;Does any slice actually create a one-way upgrade boundary, and is that flagged and accepted before merge rather than discovered during a production rollback;Is make test-fast-parallel a sufficient gate to call a slice independently deployable, given the slices change loader, doctor, and runtime-state paths that fast unit tests may not exercise|A slice labeled independently deployable actually depends on a later slice such as slice 2 doctor changes assuming the slice 4 coordinator;The no-Maintenance production-loader gate is claimed on a test-only loader despite the explicit prohibition;The downgrade path for runtime-state migration markers is undefined when reverting to an older binary
bootstrap-extraction|1|Hiroshi Tanabe|go embed removal, non-nil empty fs.FS, fstest.MapFS fixtures, GC_BOOTSTRAP=skip closure, hidden-dependency inventory|After removing the packs embed directive, is there a compile-time guarantee no path imports the deleted bootstrap Core package, or only a string-guard test that can be evaded;Is there a test proving a GC_BOOTSTRAP=skip command still materializes and validates Core through systempacks, and is the narrowed fixture-only semantics enforced rather than merely documented;Is the hidden-dependency inventory generated and complete, or a hand-list that can miss a consumer such as generated docs or a hook overlay path and break the build mid-slice|The empty bootstrap filesystem defaults to nil somewhere and panics, or a fixture still reads production Core;GC_BOOTSTRAP=skip remains a production Core bypass despite the narrowing claim;A hidden dependency outside the named inventory breaks the Core extraction slice
runtime-state-migration|1|Yelena Markovic|jsonl state and archive migration, spawn-storm ledgers, migration-completed marker, non-destructive copy, downgrade continuity|If an old binary writes legacy Maintenance state after the migration-completed marker exists during concurrent old and new binary operation, does the new binary silently read stale Core state;Is copying the jsonl-archive directory with its git remotes and refs actually safe against in-flight pushes and lockfiles, and is that copy atomic;Is there a round-trip test that an in-progress archive push with escalation fields survives migration without duplicate or dropped pushes|Concurrent old-binary writes after the marker cause silent divergence or data loss;The copy-when-destination-absent rule races two processes that both observe an absent destination;An interrupted archive copy leaves a half-copied state that the next run treats as complete
docs-operator-dx|1|Claire Dubois|canonical system-packs reference, executable wording matrix, docs lint, doctor and tutorial goldens, maintenance terminology disambiguation|Is the wording-matrix disambiguation of retired Maintenance pack versus store maintenance versus Core maintenance_worker versus stale paths actually decidable per hit, or will it produce false positives that block unrelated edits;What prevents a behavior slice from shipping stale operator wording by marking itself non-release indefinitely;Is there an end-to-end operator upgrade narrative of what an upgrader actually sees and does, or only internal lint contracts|The matrix flags legitimate lowercase maintenance English and store maintenance, creating lint noise that gets suppressed wholesale;The non-release slice escape becomes a permanent docs-debt parking lot;No single page walks an operator through the upgrade because the canonical reference is a spec not a guide
decomposition-readiness|1|Gabriel Okonkwo|schema conformance, decomposition readiness, open-questions honesty, prerequisite-artifact gating, scope realism|The plan declares Open Questions None while deferring ownership audits such as mol-review-quorum and the Codex overlay to the candidate slice, so are those decided or merely relabeled as gates that still block bead creation;Given the plan presumes generated artifacts and a gascity-packs branch as prerequisites, should the status be blocked-prerequisite per the schema rather than ready to decompose since beads cannot be built until those generators exist;Is the DAG genuinely parallelizable into independent beads, or do the cross-slice and cross-repo couplings make every bead transitively depend on the manifest and pin so the work is effectively serial|Open Questions None is inaccurate because ownership decisions are deferred rather than resolved;The artifact is dominated by attempt-contract prose over the schema design sections, which the schema says iteration must not become;Prerequisite generators are assumed to exist, so the plan should be blocked-prerequisite rather than decomposition-ready
